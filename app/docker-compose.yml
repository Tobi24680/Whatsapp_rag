version: '3.8'

services:
  whatsapp-rag:
    build: .
    container_name: whatsapp-rag-bot
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
